# Clipboard 
# bindsym $mod+v exec rofi -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}'
exec --no-startup-id greenclip daemon>/dev/null
bindsym $mod+Shift+v exec --no-startup-id rofi -modi "clipboard:greenclip print" -show clipboard

# Copy to clipboard
#bindsym $mod+c exec xsel --clipboard --input < /dev/stdin

# Paste from clipboard
#bindsym $mod+v exec xsel --clipboard --output | wl-copy

# Start greenclip daemon
#exec --no-startup-id greenclip daemon >/dev/null

# Keybinding for showing clipboard history
#bindsym $mod+v exec --no-startup-id rofi -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}'
